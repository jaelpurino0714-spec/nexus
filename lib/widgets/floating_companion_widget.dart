import 'package:flutter/material.dart';

class FloatingCompanionWidget extends StatelessWidget {
  const FloatingCompanionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
