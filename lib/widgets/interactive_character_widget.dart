import 'package:flutter/material.dart';

class InteractiveCharacterWidget extends StatelessWidget {
  const InteractiveCharacterWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
